ignores:
  # Type-only; depcheck cannot see `types` in tsconfig.
  - '@types/node'
  # size-limit resolves its preset by name from .size-limit.json.
  - '@size-limit/preset-small-lib'
  # depcheck misparses rimraf's bin-only usage in `clean` / `nuke`.
  - rimraf
  # vitest coverage provider, selected by name in vitest.config.ts.
  - '@vitest/coverage-v8'
